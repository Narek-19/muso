import React from "react";
import * as Styled from "./styled";
import AudioPlayer from "./AudioPlayer";

export const Footer = () => {
  return (
    <Styled.Footer>
      <AudioPlayer />
    </Styled.Footer>
  );
};
