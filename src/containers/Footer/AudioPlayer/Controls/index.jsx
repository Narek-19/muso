import React from "react";
import * as Styled from "./styled";
import Buttons from './Buttons';

export const Controls = () => {
  return(
  <Styled.Controls>
    <Buttons/>
  </Styled.Controls>
  );
};
